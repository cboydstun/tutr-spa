import { OnlyInstructorsGuard } from './_guards/only-instructors.guard';
import { OnlyLoggedInUsersGuard } from './_guards/only-logged-in-users.guard';

export {
	OnlyInstructorsGuard,
	OnlyLoggedInUsersGuard
};