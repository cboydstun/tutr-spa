import { CursorAtRestDirective } from './cursor-at-rest.directive';

describe('CursorAtRestDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorAtRestDirective();
    expect(directive).toBeTruthy();
  });
});
